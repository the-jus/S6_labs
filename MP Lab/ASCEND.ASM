ASSUME CS: CODE, DS: DATA

DATA SEGMENT
ARR DB 46H, 56H, 66H, 58H, 56H
LEN DW $-ARR


DATA ENDS

CODE SEGMENT

START:  MOV AX, DATA
        MOV DS, AX
	LEA SI, ARR
	MOV AX, LEN
 L3:	MOV DI, SI
	INC DI
	MOV CX, AX
	DEC CX

 L2: MOV BH, ARR [SI]
  	MOV BL, ARR [DI]
	CMP BH, BL
	JC L1
	MOV ARR [SI], BL
	MOV ARR [DI], BH
 L1: INC DI
	LOOP L2
	INC SI
	DEC AX
	CMP AX, 0001H
	JNZ L3
	

        MOV AH, 4CH
        INT 21H
CODE ENDS
        END START
	

